TODO:

- Os's specific compile-time defines
- Wrap VkResult errorcode
- Config for building without backend
- Better linker ? Improve compile times..
- Change the input system
- Remove the amount of layers
- Config files ?



DONE:
* Change backend easily
* Filesystem's API layer
* Build system using the Filesystem's api layer
